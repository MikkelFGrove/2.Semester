package multithreading;

import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;

public class ThreadDemo {
    public static void main (String [] args) {

        /*
        Logger g=Logger.getInstance();

        DepositTask deposit = new DepositTask("001",200,g);
        WithdrawTask withdraw = new WithdrawTask("002",500,g);
        TransferTask transfer = new TransferTask("005","001", 1000,g);


        Thread t1=new Thread(deposit,"Deposit Thread::");
        Thread t2=new Thread(withdraw,"Withdraw Thread::");
        Thread t3=new Thread(transfer,"Transfer Thread::");


        t1.start();
        t2.start();
        t3.start();*/
    }
}