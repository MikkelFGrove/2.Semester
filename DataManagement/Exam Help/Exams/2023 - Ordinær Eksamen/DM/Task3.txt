Relational databases use primary keys and foreign keys to enforce relationship constraints. This one uses SQL as their language, while the others are classified as nosql database which might also use sql, but also other languages. Wide-Column are where data is stored in coloumns rather then rows, which for large simple datasets can be ideal. While graph databases store data as nodes